```bash
# Déploiement sur firebase
hugo && firebase deploy --project clean-code-52d07
```

```bash
# Installation du thème hugo-academic en submodule
git submodule add -b master git@github.com:gcushen/hugo-academic.git themes/academic
```